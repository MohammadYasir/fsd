server:
    port: 8300
spring:
    rabbitmq:
        host:localhost
        port:5672
        username:guest
        password:guest
eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:7900/eureka
logging:
    level:
        org:
            springframework:
                security: DEBUG